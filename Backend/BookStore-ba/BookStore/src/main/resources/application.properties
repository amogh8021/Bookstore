spring.application.name=BookStore

# MySQL DB Config
spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/BookStoreLive}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:80213280@Study}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA Config
spring.jpa.hibernate.ddl-auto=update
logging.level.org.hibernate.type.descriptor.sql=TRACE
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
logging.level.org.hibernate.SQL=DEBUG

# JWT Key
jwt.secret.key=${JWT_SECRET:thisisthesecretkeyaddedtomyapplicationforthejwtyoubetterunderstand}

# SMTP configuration
spring.mail.host=${SMTP_HOST:smtp-relay.brevo.com}
spring.mail.port=${SMTP_PORT:587}
spring.mail.username=${SMTP_USERNAME:9105b2001@smtp-brevo.com}
spring.mail.password=${SMTP_PASSWORD:VvkMzwFbPOHjgUfm}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.protocol=smtp
spring.mail.properties.mail.smtp.from=amoghdev8021@gmail.com

# OAuth configuration
spring.security.oauth2.client.registration.google.client-id=290179939096-tf40jeajock1q17r6bkqmtk045rgnb1p.apps.googleusercontent.com

# CORS Configuration matched with frontend ports
cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:3000,http://localhost:5173,http://localhost:5174,http://localhost:5175,http://localhost:5176,http://127.0.0.1:8081}
